<% if @post.errors.any? %>
  <div>
    <h4>The following errors prevented the <%= @post.class.name.downcase %> from being saved</h4>
    <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %> 